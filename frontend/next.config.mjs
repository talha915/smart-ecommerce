/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com', 'm.media-amazon.com'], // Add your allowed image domains here
    },
  };
  
  export default nextConfig;
  